// Tailwind is here for the vendored workbench, which is built as this app's
// second page. The shell's own styling is plain CSS over the Atlas tokens.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
